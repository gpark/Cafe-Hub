<h3>All Employees</h3>
<div class="row">
<div class="col-md-2">
<div class="list-group">
<% for user in @users %>
    <%= link_to(user.name, '/users/'+user.id.to_s, { :class=>"list-group-item"}) %>
<% end %>
</div></div></div>